# Swift / SwiftPM build output
.build/
*.swiftpm

# Packaged artifacts (distributed via GitHub Releases, not committed)
UsageMeter.app/
*.dmg

# macOS / Python junk
.DS_Store
__pycache__/
*.pyc

# Local editor / tool settings
.claude/

# Secrets — real values stay local; commit Secrets.example.swift only
Sources/UsageMeter/Secrets.swift
